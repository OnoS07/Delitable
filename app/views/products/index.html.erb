<div class="container" style="padding-bottom: 100px;">
	<div class="row">
		<%= render "products/genre_sidebar", genres: @genres %>
		<div class="col-lg-10 col-12 col-sm-10">
			<span class="form-title">
				<i class="fas fa-seedling"></i> <%= @index_title %>
				<% if params[:q] %>
					<span style="font-size: 15px;font-weight: bold;">検索: [<%= @search.name_or_introduction_cont %>]
					</span>
				<% end %>
			</span>
			<% if params[:q] or params[:genre_id] %>
				<span style="margin-left: 10px;font-size: 15px;">
					<%= link_to ">back", products_path %>
				</span>
			<% end %>
			<span style="float: right">
				<%= search_form_for @search, url: products_path do |f| %>
					<%= f.search_field :name_or_introduction_cont, class:"fa", placeholder: "\uf002 キーワード" %>
					<%= f.submit "検索" %>
				<% end %>
			</span>
			<div class="product-contents"  style="padding-top: 20px">
				<% @products.each do |product| %>
					<div class="product-content">
						<%= link_to product_path(product) do %>
							<%= attachment_image_tag product, :product_image, format:"jpg",
								fallback:"top_vegetable.jpg", class:"product-image" %>
						<p>
							<i class="fas fa-seedling"></i><%= product.name %>：<br class="br-sp"><br class="br-tb">
							¥<%= product.price %>円
						</p>
						<% end %>
					</div>
				<% end %>
			</div>
		</div>
	</div>
</div>
